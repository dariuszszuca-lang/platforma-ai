# AI RESET Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Zbudować lokalną, interaktywną wersję materiału AI RESET i pokazać ją Darkowi bez publikowania.

**Architecture:** Semantyczny dokument HTML korzysta z osobnego arkusza stylów i niewielkiego modułu JavaScript w formacie UMD. Czyste funkcje odpowiadają za normalizację lokalnego stanu i postęp, a inicjalizacja przeglądarkowa obsługuje checklisty, pola, kopiowanie oraz druk.

**Tech Stack:** HTML5, CSS3, vanilla JavaScript, Node.js `node:test`, lokalny Chrome.

---

### Task 1: Kontrakt materiału i zachowania

**Files:**
- Create: `tests/ai-reset.test.js`
- Create: `assets/ai-reset.js`

- [x] **Step 1: Write failing tests**

```js
const test = require('node:test');
const assert = require('node:assert/strict');
const {
  calculateProgress,
  normalizeChecklistState,
} = require('../assets/ai-reset.js');

test('postęp zwraca zaokrąglony procent', () => {
  assert.equal(calculateProgress(3, 8), 38);
});

test('postęp pustej listy wynosi zero', () => {
  assert.equal(calculateProgress(0, 0), 0);
});

test('stan checklisty zachowuje tylko znane klucze boolean', () => {
  assert.deepEqual(
    normalizeChecklistState({ a: true, b: 'yes', obcy: true }, ['a', 'b']),
    { a: true, b: false },
  );
});
```

- [x] **Step 2: Run tests and verify RED**

Run: `node --test tests/ai-reset.test.js`  
Expected: FAIL because `assets/ai-reset.js` does not exist.

- [x] **Step 3: Implement minimal pure functions**

```js
(function (root, factory) {
  const api = factory();
  if (typeof module === 'object' && module.exports) module.exports = api;
  if (root) root.AIReset = api;
}(typeof globalThis !== 'undefined' ? globalThis : this, function () {
  function calculateProgress(done, total) {
    if (!Number.isFinite(total) || total <= 0) return 0;
    const safeDone = Math.min(Math.max(Number(done) || 0, 0), total);
    return Math.round((safeDone / total) * 100);
  }

  function normalizeChecklistState(raw, validKeys) {
    const source = raw && typeof raw === 'object' ? raw : {};
    return validKeys.reduce((state, key) => {
      state[key] = source[key] === true;
      return state;
    }, {});
  }

  return { calculateProgress, normalizeChecklistState };
}));
```

- [x] **Step 4: Run tests and verify GREEN**

Run: `node --test tests/ai-reset.test.js`  
Expected: 3 tests pass.

### Task 2: Semantyczna treść workbooka

**Files:**
- Create: `ai-reset.html`
- Modify: `tests/ai-reset.test.js`

- [x] **Step 1: Add static contract tests**

Test ma sprawdzić `noindex`, osiem sekcji modułowych, pięć kart asystentów, przyciski kopiowania, przycisk druku oraz brak wyrażenia `małe firmy`.

- [x] **Step 2: Run tests and verify RED**

Run: `node --test tests/ai-reset.test.js`  
Expected: FAIL because `ai-reset.html` does not exist.

- [x] **Step 3: Create the semantic document**

Dokument zawiera dokładną strukturę zatwierdzoną w specyfikacji, linkuje `/assets/ai-reset.css` i `/assets/ai-reset.js`, używa `main`, `section`, `article`, `details`, `label` oraz przycisków z nazwami dostępnymi dla czytników.

- [x] **Step 4: Run tests and verify GREEN**

Run: `node --test tests/ai-reset.test.js`  
Expected: all static and functional tests pass.

### Task 3: Interakcje i stan lokalny

**Files:**
- Modify: `assets/ai-reset.js`
- Modify: `tests/ai-reset.test.js`

- [x] **Step 1: Add failing storage tests**

```js
test('niepoprawny JSON stanu zwraca pusty obiekt', () => {
  assert.deepEqual(parseStoredObject('{źle'), {});
});

test('poprawny JSON obiektu zostaje odczytany', () => {
  assert.deepEqual(parseStoredObject('{"a":true}'), { a: true });
});
```

- [x] **Step 2: Run tests and verify RED**

Run: `node --test tests/ai-reset.test.js`  
Expected: FAIL because `parseStoredObject` is not exported.

- [x] **Step 3: Implement storage and browser initialization**

```js
function parseStoredObject(value) {
  try {
    const parsed = JSON.parse(value || '{}');
    return parsed && typeof parsed === 'object' && !Array.isArray(parsed) ? parsed : {};
  } catch (_) {
    return {};
  }
}
```

Inicjalizacja zapisuje checklisty i pola pod kluczami `ai-reset-checklist-v1` oraz `ai-reset-context-v1`, aktualizuje pasek postępu, kopiuje tekst z elementu wskazanego przez `data-copy-target`, obsługuje `window.print()` i czyści wyłącznie klucze AI RESET.

- [x] **Step 4: Run tests and verify GREEN**

Run: `node --test tests/ai-reset.test.js`  
Expected: all tests pass.

### Task 4: Warstwa wizualna i druk

**Files:**
- Create: `assets/ai-reset.css`

- [x] **Step 1: Implement design tokens and responsive layout**

Arkusz używa tokenów `--reset-bg`, `--reset-paper`, `--reset-ink`, `--reset-muted`, `--reset-copper`, `--reset-line`, skali odstępów 8 px, dwóch kolumn od 1024 px i jednej kolumny poniżej.

- [x] **Step 2: Implement interaction and accessibility states**

Każdy przycisk i kontrolka ma minimum 44 px, `:focus-visible`, czytelny hover/pressed state, a animacje są wyłączane w `prefers-reduced-motion`.

- [x] **Step 3: Implement print stylesheet**

`@media print` ustawia jasne tło, ukrywa przyciski, sticky rail i komunikaty, rozwija treści instrukcji i zachowuje czytelne podziały stron.

### Task 5: Weryfikacja i podgląd

**Files:**
- Verify: `ai-reset.html`
- Verify: `assets/ai-reset.css`
- Verify: `assets/ai-reset.js`
- Verify: `tests/ai-reset.test.js`

- [x] **Step 1: Run all AI RESET tests**

Run: `node --test tests/ai-reset.test.js`  
Expected: zero failures.

- [x] **Step 2: Start a local static server**

Run: `python3 -m http.server 4173`  
Expected: page available at `http://127.0.0.1:4173/ai-reset.html`.

- [x] **Step 3: Capture desktop and mobile screenshots**

Use local Chrome at 1440 px and 375 px. Confirm no horizontal overflow and all primary content remains readable.

- [x] **Step 4: Inspect browser console and print preview**

Expected: no JavaScript errors; print view does not contain helper controls.

- [x] **Step 5: Stop before publication**

Do not add the page to navigation, sitemap or Vercel deployment. Present local files and screenshots to Darek.

- [ ] **Step 6: Commit after Darek approves**

```bash
git add ai-reset.html assets/ai-reset.css assets/ai-reset.js tests/ai-reset.test.js docs/superpowers/specs/2026-07-25-ai-reset-design.md docs/superpowers/plans/2026-07-25-ai-reset.md
git commit -m "feat: add AI RESET interactive workbook"
```
